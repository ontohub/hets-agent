#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'pry'
require 'hets-agent'

%w(INT TERM).each do |signal|
  Signal.trap(signal) do
    exit
  end
end

HetsAgent::Application.boot
HetsAgent::Subscriber.new.call
