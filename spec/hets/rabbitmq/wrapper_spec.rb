require 'spec_helper'

describe Hets::Rabbitmq::Wrapper do
  it 'has a version number' do
    expect(Hets::Rabbitmq::Wrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
